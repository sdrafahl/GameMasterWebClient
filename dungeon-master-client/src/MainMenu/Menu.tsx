import * as React from 'react';

import { Button } from '@material-ui/core';

const MainMenu = () => {
    return (
        <div>
            <Button variant="contained">Join a Game</Button>
            <Button variant="contained">Start a Game</Button>
        </div >
    );
};


export default MainMenu;
